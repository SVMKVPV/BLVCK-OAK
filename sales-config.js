'use strict';

// Add the Australian mobile number in international format (for example 614xxxxxxxx)
// after the business confirms which number should receive public enquiries.
window.BLACK_OAK_SALES = Object.freeze({
  whatsappNumber: '',
});
